# Regras de análise estática (lint) do projeto.
# Base nas recomendações oficiais + algumas regras extras que reforçam
# Clean Code e consistência — coerente com a exigência do TCC.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart" # arquivos gerados (json_serializable/injectable)
    - "**/*.config.dart"
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_locals: true
    always_declare_return_types: true
    avoid_print: true
    require_trailing_commas: true
    sort_child_properties_last: true
    use_key_in_widget_constructors: true
    directives_ordering: true
